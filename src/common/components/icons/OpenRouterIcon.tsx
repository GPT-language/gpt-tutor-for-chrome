import { IconBaseProps } from 'react-icons'
import openRouterSrc from '@/common/assets/images/openrouter.png'

export function OpenRouterIcon(props: IconBaseProps) {
    return <img src={openRouterSrc} alt='OpenRouter' height={props.size} width={props.size} />
}
